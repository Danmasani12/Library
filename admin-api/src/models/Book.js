class Book {
    constructor(title, author, publisher, category, isAvailable, returnDate) {
      this.title = title;
      this.author = author;
      this.publisher = publisher;
      this.category = category;
      this.isAvailable = isAvailable;
      this.returnDate = returnDate;
    }
  }
  
  module.exports = Book;
  